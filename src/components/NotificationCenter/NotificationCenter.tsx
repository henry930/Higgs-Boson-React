// Simplified NotificationCenter since we're using static data and don't need complex notifications
const NotificationCenter = () => {
  // For static site, we don't need complex notification management
  // This can be expanded later if needed
  return null;
};

export default NotificationCenter;
