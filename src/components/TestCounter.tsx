/**
 * 🚀 簡單測試任務 - 讓 Copilot 完成這個功能
 * 
 * 任務：創建一個簡單的計數器組件
 * 
 * 要求：
 * 1. React 19 + TypeScript
 * 2. 使用 useState Hook
 * 3. 包含 +1, -1, Reset 按鈕
 * 4. 顯示當前計數值
 * 5. 使用 Tailwind CSS 樣式
 * 6. 添加適當的 ARIA 標籤
 * 
 * 請 Copilot 完成以下程式碼：
 */

import React from 'react'

interface CounterProps {
  initialValue?: number
}

export const Counter: React.FC<CounterProps> = ({ initialValue = 0 }) => {
  // Copilot: 請完成這個組件的實現
  
  return (
    <div>
      {/* Copilot: 請實現 UI */}
    </div>
  )
}

export default Counter
